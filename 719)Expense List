//c
#include <stdio.h>

int main() {
	// your code goes here
	int t;
	scanf("%d",&t);
	while(t--){
	    int n,x;
	    scanf("%d %d",&n,&x);
	     int saved_amount = 1 << (x - n);
	    
	    printf("%d\n", saved_amount);
	}

}

//python
# cook your dish here
t=int(input())
for i in range(t):
    n,x=map(int,input().split())
    count=(2**x)-((2**x)//2)
    for j in range(1,n):
        count=count//2
    print(count)
