//c
#include <stdio.h>

int main() {
	// your code goes here
	int t;
	scanf("%d",&t);
	while(t--){
	    int W,X,Y,Z;
	    scanf("%d %d %d %d",&W,&X,&Y,&Z);
	    if (W == X || W == Y || W == Z ||
        W == X + Y || W == X + Z || W == Y + Z ||
        W == X + Y + Z){
            printf("yes\n");
        }
        else{
            printf("no\n");
        }
	    
	}

}

//python
# cook your dish here
t=int(input())
for i in range(t):
    W,X,Y,Z=map(int,input().split())
    if (W == X or W == Y or W == Z or
        W == X + Y or W == X + Z or W == Y + Z or
        W == X + Y + Z):
        print("YES")
    else:
        print("NO")
