//c
#include <stdio.h>

int main() {
	// your code goes here
	int t;
	scanf("%d",&t);
	while(t--){
	    int a,b,x,y;
	    scanf("%d %d %d %d",&a,&b,&x,&y);
	    if((a-y)<=b && b<=(a+x)){
	        printf("yes\n");
	    }
	    else{
	        printf("no\n");
	    }
	}

}

//python
# cook your dish here
t=int(input())
for i in range(t):
    a,b,x,y=map(int,input().split())
    if(a-y<=b<=a+x):
        print("yes")
    else:
        print("no")
