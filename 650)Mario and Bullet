//c
#include <stdio.h>

int main() {
	// your code goes here
	int t;
	scanf("%d",&t);
	while(t--){
	    int x,y,z;
	    scanf("%d %d %d",&x,&y,&z);
	    if(0>z-(y/x)){
	        printf("%d\n",0);
	    }
	    else{
	        printf("%d\n",z-(y/x));
	    }
	}

}

//python
